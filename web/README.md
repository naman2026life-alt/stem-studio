# Stem Studio web

The Vercel-ready Next.js interface for Stem Studio. It uploads directly to the temporary processor API, polls job progress, and previews or downloads the resulting stems. See the repository root README for local setup, processor deployment, environment variables, and architecture notes.

```bash
cp .env.example .env.local
npm install
npm run dev
```
