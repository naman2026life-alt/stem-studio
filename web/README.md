# Stem Studio web

The Vercel-ready Next.js interface for Stem Studio. See the repository root README for Supabase migration, environment variables, worker setup, and deployment instructions.

```bash
cp .env.example .env.local
npm install
npm run dev
```
